# 大模型应用与开发工程面试介绍文档

本文档用于面试表达，不是完整技术白皮书。

目标：
- 只讲强相关技术点
- 只保留便于回答、便于追问展开的内容
- 避免把自己带进难以收束的底层细节
- 预设高频问题与参考回答

适用方向：
- 大模型应用工程
- AI Agent 应用开发
- AI 产品工程化
- RAG / Prompt / Tool Use / 应用集成
- iOS 或客户端智能应用工程

---

## 1. 一句话项目定位

我做的是一个以梦境解读为核心的 AI 应用，重点不是“单次文本生成”，而是把大模型、Agent 编排、Skills、工具调用和本地知识库结合起来，让它能基于用户历史梦境、塔罗规则、天气地点和时令上下文做更稳定的个性化解读。

---

## 2. 30 秒自我介绍版本

我最近重点做的是一个 AI 原生应用的工程化落地。这个项目里，我不是只接了一个大模型接口做问答，而是把整个链路拆成了几层：模型调用层、Agent 编排层、Skills 能力层和本地 RAG 知识库。这样做的目标是让回答更可控，也更贴近用户自己的历史数据，而不是完全依赖模型即兴发挥。

---

## 3. 1 分钟项目介绍版本

这个项目是一个梦境解读应用。用户输入梦境后，系统不仅会调用大模型生成解释，还会先做本地编排：检索历史梦境、检索塔罗知识、读取天气地点和时令信息，再把这些结果交给 Agent 统一组织回答。

工程上我主要做了四件事：
- 把艺术风格从普通配置升级成标准化的 `skills`
- 设计了一个轻量 agent runtime，支持问题分类、tool 调用和 skill 选择
- 做了本地 RAG，把梦境记忆和塔罗规则存成可检索知识库
- 把正式塔罗流程接入 agent，而不是只做一个简化版后台抽牌

最终效果是：这个应用不是“一个 prompt 对一个回答”，而是一个可控的、多阶段的大模型应用。

---

## 4. 3 分钟技术介绍版本

如果从架构上看，这个项目主要有四层。

第一层是模型调用层。我统一封装了大模型调用客户端，文本生成、图像生成都从这一层走，这样后续替换模型或者做 fallback 会更容易。

第二层是 Agent 编排层。这里不是直接把用户输入丢给模型，而是先做本地问题分类，判断这是解梦问题、塔罗知识问题还是环境上下文问题，然后决定要调用哪些 tools、哪些 skills。

第三层是 Skills 层。我把应用内部能力做了标准化，比如梦境检索、塔罗检索、梦境解读、行动建议，以及艺术风格本身，都变成可注册、可选择、可执行的 skill。这样它不是散落在业务逻辑里的 if/else，而是一个更清晰的能力系统。

第四层是本地知识库，也就是轻量 RAG。梦境记忆和塔罗规则会转成文档并做本地向量化，查询时先做本地召回，再把结果送进 agent。这保证了回答能利用用户自己的历史数据，而不是只靠模型的通用知识。

我比较看重的不是“回答写得像不像人”，而是整个系统是否可控、可解释、可扩展。比如结构化问题下，我会先本地产生草稿，再让大模型润色；如果模型漏掉关键段落，就自动回退到本地草稿。这种做法更像工程系统，而不是一次性的 prompt 实验。

---

## 5. 推荐主动讲的技术点

面试里建议优先讲下面这些点，因为它们既强相关，又容易展开：

### 5.1 为什么不能只用一个 prompt 直接问模型

推荐说法：
如果只是一个 prompt 直接问模型，回答会更自由，但稳定性差，也很难强制利用用户自己的历史数据。我的做法是先在本地完成工具调用、知识检索和问题分类，再把结构化上下文交给模型，模型主要负责表达，而不是负责整个系统决策。

### 5.2 为什么做 Skills

推荐说法：
因为应用里有多种能力，比如风格控制、梦境检索、塔罗检索、解释和建议，这些能力如果都写死在 orchestrator 里，后期很快会失控。skill 化之后，每个能力都有自己的定义、输入输出和关键词描述，更容易选择、组合和扩展。

### 5.3 为什么做本地 RAG

推荐说法：
这个场景很依赖用户自己的历史梦境，而这些数据不适合完全依赖远程知识库。做本地 RAG 的好处是响应更快、隐私更好，也更适合移动端。同时塔罗规则本身是相对稳定的知识，也适合本地索引。

### 5.4 Agent 的价值是什么

推荐说法：
我理解的 agent 不是“会自己规划很多步”才叫 agent，而是系统能根据问题类型做路由、调用不同工具、整合多源上下文，并输出一个更像任务完成结果的回答。这个项目里，agent 的核心价值是编排，而不是炫技式多轮推理。

### 5.5 如何提升输出稳定性

推荐说法：
我做了几层控制：本地分类、本地 tool plan、本地结构化模板、本地 fallback。尤其结构化问题下，模型不是自由生成，而是润色一份本地草稿；如果少段落，就直接回退。这样稳定性比纯 prompt 明显高。

### 5.6 如何平衡工程复杂度

推荐说法：
我没有上太重的基础设施，比如远程向量库、多 agent 调度框架、复杂 planner，而是先用轻量本地方案验证产品路径。对移动端应用来说，这是更现实的工程取舍。

---

## 6. 不建议主动展开的细节

这些内容不是不能答，而是不建议一开始主动讲：

- 向量维度为什么是 192
- 哈希 embedding 的数学细节
- 每个 prompt 的完整内容
- 所有风格 skill 的细粒度差异
- SwiftUI 页面层实现
- 所有工具的关键词评分细节

原因很简单：这些点容易把面试带进“细但不值钱”的方向，且不如架构、稳定性、工程取舍、产品理解更加分。

---

## 7. 面试中可以反复强调的工程价值

- 我做的不是单次 prompt，而是多层可控的大模型应用。
- 我把“模型能力”和“应用编排”明确拆开了。
- 我重视本地上下文、用户历史数据和输出稳定性。
- 我会优先做工程上能落地的方案，而不是一开始堆很重的 AI 基建。
- 我会考虑 fallback、可观测性和后续扩展性。

---

## 8. 高频问题与参考回答

下面是 20 个高频问题，答案刻意写成“面试可直接说”的风格。

### Q1：你这个项目里，大模型主要负责什么？

A：
大模型主要负责生成和表达，不负责整个系统决策。系统决策比如问题分类、知识检索、工具调用和结构化草稿，尽量先在本地完成。这样模型的角色更像“最后一跳的合成器”，而不是全权黑盒。

### Q2：为什么不直接做一个聊天机器人？

A：
因为这个产品场景不是通用聊天，而是需要结合用户历史梦境、塔罗规则和现实环境上下文。如果只做聊天机器人，很难保证这些上下文被稳定利用，也很难保证输出结构一致。

### Q3：你理解的 Agent 是什么？

A：
我理解的 agent 重点不在“会不会自我意识式思考”，而在于能不能根据任务做路由、调用工具、整合上下文并完成一个更复杂的结果交付。这个项目里的 agent 本质上是一个应用层编排器。

### Q4：你为什么要做 skills？

A：
因为系统里存在多种能力单元，比如梦境检索、塔罗检索、解释、建议、风格控制。如果不 skill 化，它们会散落在业务逻辑里。skill 化以后，能力边界更清晰，也更容易复用和扩展。

### Q5：skills 和 tools 的区别是什么？

A：
tool 更像直接访问某种数据源或环境能力，比如查天气、查地点、查知识库；skill 更像对上下文做更高层加工的能力，比如解释、建议、风格控制。tool 偏取数，skill 偏处理。

### Q6：你的 RAG 是怎么做的？

A：
我做的是轻量本地 RAG。把梦境记忆和塔罗规则转成文档，做本地向量化，然后按 query 检索 topK 结果。这样不依赖远程向量库，更适合移动端场景，也能更好地利用用户私有数据。

### Q7：为什么不用标准 embedding API？

A：
主要是工程取舍。这个应用优先需要本地可用、低依赖、对移动端友好，所以先用了轻量本地 embedding 方案。它不如大模型 embedding 精准，但足够支持当前场景的检索增强，成本和复杂度更可控。

### Q8：你怎么保证模型回答稳定？

A：
我不会把稳定性全押在 prompt 上。做法是：先本地分类、先本地召回、先本地组结构化草稿，再让模型润色。最后还会校验模型输出，如果缺关键 section，就直接回退本地草稿。

### Q9：你怎么做问题分类？

A：
当前用的是本地规则和关键词意图识别，而不是把分类交给模型。因为这个问题类型相对有限，本地判定更快、更便宜，也更可控。

### Q10：这个项目里最关键的技术决策是什么？

A：
最关键的是把“应用编排”从“模型生成”里拆出来。很多大模型应用的问题都出在把所有任务都交给模型，这样会让系统不可控。我这里优先保证应用层自己知道为什么调用什么。

### Q11：你做过哪些 fallback 设计？

A：
主要有三类：模型不可用时的本地 fallback、结构化回答缺段落时的草稿 fallback、正式塔罗结果不可用时的简化抽牌 fallback。我的原则是，用户体验可以退化，但不能完全断。

### Q12：你怎么做可观测性？

A：
我保留了 skill trace 和 tool trace，可以看到这轮回答调用了哪些能力、为什么调用、得分如何。这种可观测性对调试 agent 非常重要，不然很难知道回答到底是怎么来的。

### Q13：为什么把艺术风格也做成 skill？

A：
因为它不只是 UI 配置，它会影响诗歌生成、图像 prompt 和 agent 解读语气。把它做成 skill 后，它就变成了一个标准能力，而不是零散参数。

### Q14：正式塔罗流程为什么也要接入 agent？

A：
因为如果 agent 自己后台随便抽一张，和应用里的正式塔罗模块就会变成两套逻辑，结果也不一致。接入正式流程后，用户在 agent 场景下得到的是同一套抽牌、解读和存储逻辑。

### Q15：天气、地点、时令为什么也要参与解读？

A：
因为梦境解读不是只看文本本身，还要看用户当下语境。天气、地点、时令不是主结论来源，但它们能提供现实背景，尤其对建议行动这种输出很有帮助。

### Q16：你这个项目里，最像工程能力体现的地方是什么？

A：
不是调 API 本身，而是把整个链路做成了可控系统：模型、agent、skills、tools、RAG、fallback、trace 都有明确边界。这种分层和约束能力更接近真实工程，而不是 demo。

### Q17：如果数据量变大，你会怎么演进？

A：
我会优先升级两块：一是 embedding 与检索精度，二是 knowledge store 的存储和更新机制。如果规模继续扩大，再考虑远程向量库或服务化检索，而不是一开始就上重基建。

### Q18：如果让你重做一次，你会改什么？

A：
我会更早把 route、template、tool plan 配置化，减少 orchestrator 中的硬编码；同时会把 tool/skill 的选择策略做得更声明式，方便后续扩展更多能力。

### Q19：你觉得这个项目最有价值的经验是什么？

A：
最大的经验是，大模型应用的核心难点通常不在“模型会不会答”，而在“系统怎样稳定地让模型答对方向、用对上下文、输出对结构”。这是典型的工程问题，不只是 prompt 问题。

### Q20：如果面试官问你这个项目的难点，你怎么答？

A：
我会说难点有三个：第一是怎么让回答利用用户历史而不是泛泛而谈；第二是怎么让塔罗、梦境、天气、时令这些异构上下文协同工作；第三是怎么在保持生成感的同时保证结果可控。我的解决方式分别是本地 RAG、agent 编排和结构化草稿 + fallback。

---

## 9. 一个比较稳的收尾说法

如果让我总结这个项目，我会说它不是“我接了一个大模型接口做了个聊天功能”，而是我把一个大模型应用拆成了模型层、agent 层、skills 层和知识库层，并围绕稳定性、可控性和个性化做了工程化设计。这类能力我认为是做 AI 应用开发时最关键的部分。

---

## 10. 建议的使用方式

推荐这样用这份文档：

1. 背熟第 2、3、4 节
   这是你开场最常用的内容。

2. 从第 5 节挑 3 个技术点反复练
   不要试图一次讲完所有点。

3. 高频问题重点练这几题
   `Q1 / Q3 / Q4 / Q6 / Q8 / Q10 / Q16 / Q20`

4. 如果被追问太深
   优先把回答拉回“工程取舍、系统可控、用户场景价值”。

5. 不要过度强调“模型参数”
   多强调“应用工程化能力”。

---

## 11. 一句最短总结

我做的是一个“有本地上下文、有能力编排、有知识检索、有输出约束”的大模型应用，而不是一个简单的模型调用壳。
