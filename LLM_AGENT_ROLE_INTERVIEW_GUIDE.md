# AI Agent 岗位定制面试介绍文档

本文档基于如下岗位画像定制：
- 聚焦核心业务场景，推动 AI 落地
- 参与从需求洞察到系统构建的全流程
- 使用 Agent、RAG、记忆、工具编排等前沿技术
- 关注工程落地、效果闭环、稳定性与规模化演进

这份文档的目标不是“把项目所有技术点都讲完”，而是帮助你把当前项目讲成一段符合 JD 的能力证明。

---

## 1. 这类岗位最看重什么

这份 JD 真正要的不是“你会不会调一个模型 API”，而是五件事：

1. 能不能把模糊业务问题转成 AI 可以落地的问题
2. 能不能设计 Agent / RAG / 记忆 / 工具编排架构
3. 能不能把系统做成工程闭环，而不是 demo
4. 能不能做可观测、可回退、可演进的智能系统
5. 能不能兼顾效果、稳定性、性能与长期扩展

所以你的面试表达要围绕这五件事，而不是围绕“我用了哪个模型”。

---

## 2. 面试时的一句话定位

我做的不是一个简单的大模型聊天功能，而是一个围绕真实业务场景做工程化落地的 AI 应用：我把用户问题拆成可执行任务，通过 Agent 编排、Skills 能力封装、本地 RAG 和记忆上下文，把模型输出做成一个可控、可追踪、可迭代优化的系统。

---

## 3. 30 秒岗位对齐版自我介绍

我最近主要在做一个 AI 原生应用的工程化落地，重点不是单点生成，而是把业务问题抽象成一个可执行的 Agent 系统。这个项目里我做了本地 RAG、skills 体系、tool 编排、正式抽牌与环境上下文接入、结构化回答约束和 fallback 机制。我的关注点是如何让模型真正服务业务场景，而不是只做一个看起来聪明的 demo。

---

## 4. 1 分钟岗位对齐版项目介绍

这个项目表面上是一个梦境解读应用，但我实际把它当作一个 AI 业务系统来做。因为用户要的不是“文采好的回复”，而是一个能结合个人历史记忆、规则知识、现实环境和特定解读流程给出稳定结果的智能能力。

所以我做的事情主要有四类：
- 把问题从自然语言输入转成可编排任务，先分类，再决定调用哪些工具和 skills
- 搭建本地 RAG，把梦境记忆和塔罗规则做成可检索知识库
- 设计了一套轻量 agent runtime，负责 route、tool plan、上下文注入和最终回答合成
- 加上结构化输出、trace 和 fallback，让它能被调试、被观测、被持续迭代

对我来说，这个项目最重要的价值不在模型本身，而在我把一个业务需求拆成了完整的 AI 工程闭环。

---

## 5. 3 分钟深度介绍：按 JD 维度展开

### 5.1 需求理解与问题归因

这个项目的核心业务问题不是“生成一段解梦文案”，而是“如何让解读更贴近用户自己，而不是泛泛而谈”。我把这个问题继续拆成三个可落地方向：

- 用户历史梦境是否被有效利用
- 规则知识比如塔罗牌义是否稳定接入
- 回答是否能结合当前环境，而不是只看文本本身

也就是说，我先做的不是写 prompt，而是把模糊需求归因成几个清晰的 AI 子问题：记忆召回、规则召回、上下文注入、结构化输出控制。

这点很适合对齐 JD 里的“将模糊业务痛点转化为明确 AI 解决目标”。

### 5.2 架构设计

我把系统拆成四层：

1. 模型层
   统一封装大模型调用，负责生成，不负责整个系统决策。

2. Agent 编排层
   负责问题分类、route、tool plan、上下文组装和最终回答控制。

3. Skills 层
   把梦境检索、塔罗检索、梦境解读、行动建议、风格控制这些能力标准化。

4. 知识库 / 记忆层
   用本地 RAG 维护梦境记忆和塔罗规则，给 agent 提供可检索上下文。

这个架构的核心思想是：模型负责表达，系统负责决策。

### 5.3 知识与环境构建

知识部分我做了两类：

- 梦境记忆库
  把用户梦境转成结构化文档，包括原始描述、诗歌、世界名、意象、情绪、地点、天气和补充日记等内容。

- 塔罗规则库
  把牌义和牌阵规则做成本地文档索引。

环境部分我做了两类：

- 天气地点上下文
- 时令节气上下文

这样 agent 在解读时不是只看用户输入，而是拥有更完整的执行环境。

### 5.4 核心能力实现

Agent 侧我主要实现了这些能力：

- 意图识别
  区分明确解梦、塔罗知识问答、环境类问题和一般指导类问题。

- 工具编排
  根据问题类型决定是否调用梦境记忆、塔罗规则、天气地点、时令节气、正式塔罗结果等工具。

- 能力编排
  通过 skills 把检索、解释和行动建议分层执行，而不是把所有逻辑塞进一个 prompt。

- 输出约束
  对结构化问题，先本地生成草稿，再让模型润色；如果模型漏段落，就直接 fallback。

- 观测能力
  保留 skill trace 和 tool trace，用于调试、归因和后续优化。

### 5.5 系统迭代与演进

我现在这套系统虽然还是轻量版本，但已经具备持续迭代基础：

- 可以看到每轮调用了哪些工具和 skills
- 可以定位回答为什么偏离预期
- 可以调整问题分类规则
- 可以调整 RAG 内容结构和召回策略
- 可以继续把 route、tool plan、template 配置化

所以它不是一个一次性 demo，而是能持续优化的系统。

### 5.6 性能与稳定性

这个项目虽然不是超高并发服务，但我已经按工程系统思路做了几层稳态设计：

- 本地优先，减少对远程依赖
- 模型不可用时有 fallback
- 正式塔罗结果不可用时有简化兜底
- 结构化回答缺段落时有本地草稿兜底
- 工具和技能的调用链可观测

如果未来服务化，我会继续往异步化、缓存化、分层降级和自动评测方向推进。

---

## 6. 你最该主动讲的“JD 对齐点”

### 6.1 我不是只做了调用，而是做了“问题拆解 -> 系统设计 -> 能力实现 -> 结果约束”

这句话很重要，因为它能覆盖：
- 需求理解
- 架构设计
- 核心能力实现
- 系统演进

### 6.2 我做的是“AI 工程系统”，不是“Prompt 实验”

可以对齐：
- Agent
- RAG
- 记忆管理
- 工具编排
- 可观测性
- 回退机制

### 6.3 我会优先做可落地方案，而不是堆重基建

这点很适合回答工程取舍类问题。

例如：
- 为什么先做本地 RAG
- 为什么先做轻量 skills runtime
- 为什么先做单 agent 编排而不是多 agent

---

## 7. 面试时建议突出的方法论

你可以把自己的方法论概括成下面这套：

1. 先定义业务目标，不先写 prompt
2. 把目标拆成可编排的子问题
3. 用 tools 和 knowledge 提供确定性上下文
4. 用 skills 把能力标准化
5. 让 agent 负责路由与编排
6. 让模型负责表达与合成
7. 用 fallback 和 trace 做稳定性闭环
8. 后续通过 case 和评测迭代效果

这套方法论和 JD 的整体风格是高度匹配的。

---

## 8. 20 个高频岗位定制问题与回答

### Q1：你怎么理解“需求理解与归因”在 AI 项目里的作用？

A：
我理解它是整个 AI 项目的起点。很多业务方一开始说的是现象，比如“回答不够准”“用户觉得不够懂他”，但不能直接把这种话翻译成 prompt。要先归因是记忆没用上、规则知识没用上，还是输出结构不稳定。我在项目里就是先把“解梦不够个性化”拆成记忆召回、规则召回、环境注入和回答约束几个问题，再分别落地。

### Q2：你做这个项目时，业务问题是怎么被抽象成 AI 系统的？

A：
我没有把它看成一个文本生成任务，而是看成一个“多源上下文驱动的决策与表达系统”。用户输入只是入口，系统真正要完成的是：识别问题类型、召回个人历史、补充规则知识、引入现实环境，再输出稳定结果。所以我最终设计的是 agent + skills + RAG，而不是单 prompt。

### Q3：你为什么认为这个项目适合用 Agent，而不是普通 Chatbot？

A：
因为这个场景不是单轮对话，而是需要基于问题类型调用不同能力。比如解梦要查梦境记忆、天气地点、时令，还可能要走正式塔罗流程；塔罗知识问答又是另一条链路。这种带路由、带工具、带上下文注入的系统，本质上更像 agent 编排问题。

### Q4：你的 Agent 架构最核心的设计是什么？

A：
最核心的是把“决策”从“生成”中拆出来。问题分类、tool plan、skill 选择、上下文注入这些都在本地完成，大模型只负责最后的表达整合。这样系统更可控，也更容易调试和演进。

### Q5：你项目里的记忆是怎么设计的？

A：
我现在的记忆主要是“用户历史梦境记忆”，不是简单存聊天记录，而是把梦境描述、情绪、意象、世界名、地点、天气、补充文本等融合成结构化文档，再做本地检索。这样记忆不是纯时间序列，而是可召回、可利用的上下文资产。

### Q6：你的 RAG 为什么用本地方案？

A：
因为这个场景天然偏私有数据，而且是移动端应用。本地 RAG 更适合做低依赖、低延迟和隐私友好的方案。当前我优先保证“能稳定用上用户自己的历史数据”，而不是一开始就上很重的远程向量基础设施。

### Q7：你怎么优化召回质量和上下文注入策略？

A：
目前主要做了三件事：第一，文档构建时把梦境多种字段融合，提升召回信号；第二，把塔罗规则拆成牌义文档和牌阵文档；第三，不是把所有召回结果直接塞给模型，而是先经过 tools 和 skills 做摘要，再进入最终回答阶段。这样上下文更干净，也更适合模型消费。

### Q8：你怎么理解记忆、知识库和工具之间的关系？

A：
我会把它们看成三个不同层次。记忆是用户私有历史，知识库是稳定规则知识，工具是访问环境和外部能力的手段。Agent 负责把三者拼起来，形成当前任务真正可执行的上下文。

### Q9：你做了哪些工具编排？

A：
当前主要有梦境记忆查询、塔罗规则查询、时令节气上下文、天气地点上下文和塔罗抽牌结果读取。关键不只是“有工具”，而是根据问题类型决定什么时候调用、什么时候不调用，避免上下文污染。

### Q10：你做了哪些“核心能力实现”而不是业务 glue code？

A：
我认为比较核心的有五块：意图识别、tool routing、skill selection、结构化回答约束、trace 可观测性。这些都不是简单 glue code，而是 agent 系统真正能稳定运行的骨架。

### Q11：你怎么做 reflection / 纠错闭环？

A：
我当前没有上很重的自反思 planner，而是先做更工程化的闭环：先本地生成结构化草稿，再让模型润色；如果模型缺少关键 section，就自动 fallback。这是一个更轻量但很实用的纠错机制。后续如果要演进，我会在此基础上做自动评测和 case 驱动优化。

### Q12：你怎么做可观测性和归因分析？

A：
我保留了 skill trace 和 tool trace，可以看到每一轮回答用了哪些能力、调用顺序是什么、分数如何。这让问题不再只是“感觉回答不对”，而是能归因到是分类错了、召回弱了还是模板控制失效了。

### Q13：如果业务要规模化演进，你觉得先补哪块？

A：
我会先补三块：一是自动评测和 case 回放；二是 route、tool plan 和模板的配置化；三是检索质量升级，比如更好的 embedding 和召回重排。这样可以先把“优化闭环”跑起来，再谈更重的 infra。

### Q14：你做过哪些 SDK / 标准化能力沉淀？

A：
在这个项目里，skills runtime、tool 协议、统一模型客户端、本地知识文档结构都已经是标准化能力雏形。虽然还不是独立 SDK 产品，但它们已经具备可复用和可迁移的工程价值。

### Q15：你怎么理解“从单点验证到规模化落地”？

A：
单点验证关注的是“能不能跑起来”，规模化落地关注的是“能不能稳定复用”。我现在的设计重点就在后者，比如把能力做成 skill、把上下文接入做成 tool、把回答约束做成模板、把过程做成 trace，这些都是为了未来可复制而不是一次性演示。

### Q16：你怎么处理性能和稳定性问题？

A：
我会优先用分层降级思路。比如本地知识优先、本地模板优先、模型不可用有 fallback、正式抽牌不可用有退化路径、上下文是按需注入而不是全量塞入。这样的设计在性能和稳定性上都更有弹性。

### Q17：如果放到高并发服务场景，你会怎么改？

A：
我会从三个方向改：第一，把检索和部分上下文构造异步化；第二，对稳定知识和部分召回结果加缓存；第三，对不同能力设计分层降级策略。然后再补上更完整的监控指标和自动评测体系。

### Q18：你对大模型幻觉和 prompt 注入怎么看？

A：
我的思路是尽量降低模型负责的决策范围。问题分类、工具调用和知识召回尽量放在本地确定性逻辑里，模型只做最后表达。同时上下文来源尽量结构化、受控，避免把不可信输入直接提升成系统指令。

### Q19：你觉得这个项目里最能体现“技术价值转化”的点是什么？

A：
是我把一个偏内容生成的产品需求，转成了一个可观测、可控、可迭代的 AI 系统。这个转化本身就是技术价值，因为它让能力不再只依赖 prompt，而能持续优化、持续复用、持续服务业务目标。

### Q20：如果让你用一句话概括你和这个 JD 的匹配点，你会怎么说？

A：
我比较匹配的地方在于，我做的不是“模型接入”，而是围绕真实场景把需求拆成 Agent、RAG、记忆、工具编排和输出控制，并且用工程化方式把它落成一个可持续优化的系统。

---

## 9. 哪些点最值得面试里主动说

优先讲这 8 个：

1. 我是先做需求拆解，再做模型接入
2. 我把大模型应用拆成模型层、agent 层、skills 层、知识层
3. 我做了本地 RAG 和用户记忆利用
4. 我做了工具编排，而不是单 prompt
5. 我把正式业务流程接进了 agent，而不是做平行 demo
6. 我有结构化输出约束和 fallback
7. 我保留了 trace，便于归因和迭代
8. 我考虑的是长期可演进，而不是一次性效果

---

## 10. 这个岗位下最稳的收尾说法

如果让我总结，我认为我最匹配这类岗位的地方，不是我会不会调模型，而是我能把一个业务问题拆成可执行的 AI 系统，并且从 Agent、知识库、记忆、工具编排、稳定性和可观测性几个维度把它做成一个真正可落地、可迭代的工程闭环。
