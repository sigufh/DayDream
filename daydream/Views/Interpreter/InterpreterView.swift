import SwiftUI

struct InterpreterView: View {
    var body: some View {
        ZStack {
            Color.pearlWhite.ignoresSafeArea()
            Text("说书人")
                .dreamTitle()
        }
    }
}
